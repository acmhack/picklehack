import React from "react";

import styled, { AnyStyledComponent } from "styled-components";

const Example: AnyStyledComponent = styled.div``;

export default () => {
  return <Example>I am FAQ</Example>;
};
